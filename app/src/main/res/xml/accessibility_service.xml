<?xml version="1.0" encoding="utf-8"?>
<!--
    Deliberately the smallest possible service. No event types, so nothing is delivered to it; no
    canRetrieveWindowContent, so it cannot read what is on screen; no gesture capability. Its only
    use is the global action that opens the notification shade, which an app has no other
    supported way to reach.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canPerformGestures="false"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_description" />
